{
  "name": "template",
  "version": "1.0.0",
  "description": "template for testing",
  "main": "src/index.html",
  "scripts": {
    "sync": "browser-sync start -s \"src\" -f \"src/**/*.*\"",
    "sass": "node-sass -w --source-map=true src/scss/layout.scss src/scss/layout.css",
    "start": "npm-run-all -p sync sass",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Laura Pribyl",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.7",
    "node-sass": "^4.9.3",
    "npm-run-all": "^4.1.3"
  }
}
